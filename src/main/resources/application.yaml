server:
  port: 8080

spring:
  profiles:
    active: local
  application:
    name: Mapsea-Notification-Consumer
  threads:
    virtual:
      enabled: true
  cloud:
    aws:
      secretsmanager:
        enabled: false
